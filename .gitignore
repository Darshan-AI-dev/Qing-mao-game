node_modules/
dist/
dist-offline/
playwright-report/
test-results/
Qing-Mao-Rebirth-offline.zip
*.local
.DS_Store

# Visual sweep output: regenerate with npm run sweep / npm run sweep:play.
sweep/
sweep-play/
sweep-play-phone/
sweep-phone/
sheet-play*.png
sheet-wide*.png

# Throwaway visual captures. These belong in the session scratchpad, not the repo:
# six of them were committed by an `add -A` during the rendering pass.
scratchpad-shots/

# Local visual probes: throwaway specs used to shoot the game while iterating.
tests/e2e/zz-*.spec.ts
shots-tmp/
